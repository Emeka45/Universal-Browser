name: Build Universal Browser

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: universal-browser-build-${{ github.workflow_ref }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 1

      - name: Set up JDK 17
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v5
        with:
          gradle-version: '9.7.1'

      - name: Validate current browser source tree
        run: |
          set -euo pipefail
          test -f app/src/main/java/com/coeric/universalbrowser/MainActivity.kt
          test -f app/src/main/java/com/coeric/universalbrowser/BrowserTabManager.kt
          test -f app/src/main/java/com/coeric/universalbrowser/BrowserSecurityController.kt
          test -f app/src/main/java/com/coeric/universalbrowser/BrowserPermissionController.kt
          test -f app/src/main/java/com/coeric/universalbrowser/BrowserPowerCenter.kt
          test -f app/src/main/java/com/coeric/universalbrowser/BrowserFeatureCenter.kt
          test -f app/src/main/java/com/coeric/universalbrowser/DownloadCenter.kt
          test -f app/src/main/java/com/coeric/universalbrowser/DownloadService.kt
          test -f app/src/main/java/com/coeric/universalbrowser/TabStateStore.kt
          test -f app/src/main/java/com/coeric/universalbrowser/UniversalBrowserApp.kt
          grep -q 'GeckoView' app/src/main/java/com/coeric/universalbrowser/MainActivity.kt
          grep -q 'BrowserTabManager' app/src/main/java/com/coeric/universalbrowser/MainActivity.kt
          grep -q 'BrowserPermissionController.attach' app/src/main/java/com/coeric/universalbrowser/MainActivity.kt
          grep -q 'BrowserPowerCenter' app/src/main/java/com/coeric/universalbrowser/MainActivity.kt
          grep -q 'DownloadCenter' app/src/main/java/com/coeric/universalbrowser/MainActivity.kt
          grep -q 'saveAsPdf' app/src/main/java/com/coeric/universalbrowser/BrowserFeatureCenter.kt
          grep -q 'useTrackingProtection' app/src/main/java/com/coeric/universalbrowser/UniversalBrowserApp.kt
          echo 'Universal Browser source validation passed.'

      - name: Check formatting
        run: git diff --check

      - name: Assemble debug APK
        run: gradle :app:assembleDebug --no-daemon --stacktrace

      - name: Verify generated universal APK
        shell: bash
        run: |
          set -euo pipefail
          mapfile -t apks < <(find app/build/outputs/apk/debug -type f -name '*.apk' | sort)
          apk_count=${#apks[@]}
          echo "Found $apk_count debug APK(s)"
          test "$apk_count" -eq 1
          apk="${apks[0]}"
          case "$apk" in
            *universal*|*debug*) : ;;
            *) echo "Unexpected APK name: $apk"; exit 1 ;;
          esac
          file "$apk"
          du -h "$apk"
          unzip -l "$apk" > apk-contents.txt
          grep -q 'lib/armeabi-v7a/' apk-contents.txt
          grep -q 'lib/arm64-v8a/' apk-contents.txt
          echo 'Verified native libraries for armeabi-v7a and arm64-v8a.'
          echo "Universal APK verified: $apk"

      - name: Upload universal APK
        uses: actions/upload-artifact@v4
        with:
          name: universal-browser-universal-apk
          path: app/build/outputs/apk/debug/*.apk
          if-no-files-found: error
